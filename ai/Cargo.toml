[package]
name        = "fsn-manager-ai"
description = "FreeSynergy AI Manager — backend logic for local AI engine management"
version     = "0.1.0"
edition     = "2021"
license     = "MIT"

[dependencies]
serde      = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror  = "2"
