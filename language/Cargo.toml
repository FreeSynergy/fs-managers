[package]
name = "fsn-manager-language"
version = "0.1.0"
edition = "2021"
description = "FreeSynergy Language Manager — reads/writes the active language, provides UI picker component"

[lib]
name = "fsn_manager_language"
path = "src/lib.rs"

[dependencies]
